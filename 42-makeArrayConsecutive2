int makeArrayConsecutive2(std::vector<int> sequence) {
std::sort(sequence.begin(),sequence.end());
int n=sequence.size();
//for(int i=0;i<sequence.size();i++)
   // cout<<sequence[i]<<' ';
return sequence[n-1]-sequence[0]-n+1;
}
