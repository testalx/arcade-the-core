bool isSmooth(std::vector<int> arr) {
int n=arr.size();
if(n%2)
    if(arr[0]==arr[n-1]&&arr[0]==arr[n/2])
        return true;
    else return false;
else
    if(arr[0]==arr[n-1]&&arr[0]==arr[n/2-1]+arr[n/2])
        return true;
    else return false;
}
